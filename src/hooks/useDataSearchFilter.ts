import React from 'react'
// import { useSearchQuery } from '../services/searchApi';

export const useDataSearchFilter = () => {


//     const { data, error, isLoading } = useSearchQuery(1);

//     console.log('data :>> ', data);

//     // filter array of objects to three arrays of objects by using filter method of "media_type" key value
//     const movies = data?.results?.filter((movie: any) => movie.media_type === 'movie');
//     const tvShows = data?.results?.filter((tvShow: any) => tvShow.media_type === 'tv');
//     const people = data?.results?.filter((person: any) => person.media_type === 'person');

//     console.log('movies :>> ', movies);
//     console.log('tvShows :>> ', tvShows);
//     console.log('people :>> ', people);

//     const newData = {
//         "movies": movies,
//         "tvShows": tvShows,
//         "people": people
//     }


//   return (
//     {newData, error, isLoading}
//   )
}
